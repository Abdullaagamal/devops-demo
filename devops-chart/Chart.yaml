apiVersion: v2
name: devops-chart
description: Helm chart for the DevOps demo app (Express API)
type: application

# Chart version — bump when templates or values change
version: 0.1.0

# App version — matches the deployed application (see Dockerfile / package.json)
appVersion: "1.0.0"